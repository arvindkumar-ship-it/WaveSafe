[pytest]
markers =
    disaster_sim: slow, high-load scenario tests (evacuation, mass-SOS) — skipped locally, run in staging+
    offline: tests simulating weak/no network conditions
    e2e: Module 28 full reference-flow tests, run before disaster_sim in the runbook
asyncio_mode = auto
