# Backend base URL. Leave empty when the frontend is served behind the same host/proxy.
VITE_API_BASE_URL=https://wavesafe.onrender.com

# Use the documented /v1/beaches APIs by default. Set to false only for explicit local test/fallback mode.
VITE_USE_BEACH_API=true
